set(MODELS_HEADERS
    include/device_info.h
    include/measurement_data.h
    include/sensor_data.h
    include/system_config.h
    include/data_statistics.h
    include/physics_constants.h
    include/physics_calculator.h
)

set(MODELS_SOURCES
    src/device_info.cpp
    src/measurement_data.cpp
    src/sensor_data.cpp
    src/system_config.cpp
    src/physics_calculator.cpp
)

add_library(models_lib STATIC
    ${MODELS_SOURCES}
    ${MODELS_HEADERS}
)

target_include_directories(models_lib
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include
)

target_link_libraries(models_lib PUBLIC
    utils_lib  # 只依赖utils
)

target_compile_features(models_lib PUBLIC cxx_std_17)