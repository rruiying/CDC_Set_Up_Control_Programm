set(MODELS_SOURCES
    src/sensor_data.cpp
)

set(MODELS_HEADERS
    include/sensor_data.h
)

add_library(models_lib ${MODELS_SOURCES} ${MODELS_HEADERS})
target_include_directories(models_lib PUBLIC include)
target_link_libraries(models_lib
    Qt6::Core
)